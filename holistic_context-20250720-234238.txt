Project Context for app-factory-v2
Generated on: 2025-07-20T23:42:38.233151
================================================================================

--- Google Cloud Build Log (Latest) ---

No Google Cloud Build logs found or gcloud CLI is not configured.


================================================================================

--- File: .gitignore ---

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars

# Mac
.DS_Store

# Secrets
.env

================================================================================

--- File: README.md ---

# App Factory V2 - Cloud Native

This repository contains the complete infrastructure-as-code and application source for the App Factory V2 project, designed to run entirely on the Google Cloud Platform.

## Overview

The App Factory is an automated system that discovers, vets, develops, and publishes mobile applications. It operates as a collection of containerized microservices, each acting as a specialized "AI employee" within a digital assembly line. This project leverages Docker, Cloud Run, Cloud Build, and other GCP services to create a scalable and autonomous application production pipeline.

### Architecture

- **CI/CD:** Managed by Cloud Build, triggered by pushes to the `main` branch.
- **Orchestration:** Cloud Run services are defined and deployed via a `compose.yaml` file.
- **Services:** Each service (`discovery-cycle`, `cso-vetting`, etc.) runs as an independent Cloud Run service with a dedicated IAM service account.
- **Tools:** Long-running or heavy tasks (`robust-web-scraper`, `google-play-publisher`) are designed to run as Cloud Run Jobs.
- **Security:** Secrets are managed via Google Secret Manager. Service-to-service communication will be secured through IAM and service account identities.

================================================================================

--- File: cloudbuild.yaml ---

steps:
# Step 1: Build all container images locally in the build worker.
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'build']
  dir: 'backend'

# Step 2: Push the explicitly named images to Artifact Registry.
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'push']
  dir: 'backend'

# Step 3: Deploy the now-existing images from the registry to Cloud Run.
- name: 'gcr.io/google.com/cloudsdktool/google-cloud-cli'
  entrypoint: 'gcloud'
  args:
    - 'alpha'
    - 'run'
    - 'compose'
    - 'up'
    - '--region=us-central1'
  dir: 'backend'

options:
  logging: CLOUD_LOGGING_ONLY

================================================================================

--- File: holistic_context-20250718-120410.txt ---

Project Context for app-factory-v2
Generated on: 2025-07-18T12:04:10.547855
================================================================================

--- Google Cloud Build Log (Latest) ---

No Google Cloud Build logs found or gcloud CLI is not configured.


================================================================================

--- File: cloudbuild.yaml ---

steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'build']
  dir: 'backend'
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'push']
  dir: 'backend'
- name: 'google/cloud-sdk:latest'
  entrypoint: 'gcloud'
  args:
    - 'alpha'
    - 'run'
    - 'compose'
    - 'up'
    - '--file=compose.yaml'
    - '--project=app-factory-v2'
    - '--region=us-central1'
  dir: 'backend'
options:
  logging: CLOUD_LOGGING_ONLY

================================================================================

--- File: backend/.env ---



================================================================================

--- File: backend/.gitignore ---

.env


================================================================================

--- File: backend/compose.yaml ---

services:
  mcp-gateway:
    image: docker/mcp-gateway:latest
    ports:
      - "8811:8811"
    command:
      - "--transport=sse"
      - "--servers=github-tool,web-scraper-tool,play-publisher-tool"
  discovery-cycle-service:
    build:
      context: ./services/discovery_cycle
  cso-vetting-service:
    build:
      context: ./services/cso_vetting
  cpo-analysis-service:
    build:
      context: ./services/cpo_analysis
  ai-developer-agent-service:
    build:
      context: ./services/ai_developer
    x-google-secrets:
      - secret: gemini-api-key
        env: GEMINI_API_KEY
  cmo-publishing-agent:
    build:
      context: ./services/cmo_publishing
    x-google-secrets:
      - secret: google-play-api-key
        env: GOOGLE_PLAY_API_KEY
  github-tool:
    image: ghcr.io/github/github-mcp-server:latest
    x-google-secrets:
      - secret: github-token
        env: GITHUB_TOKEN
  web-scraper-tool:
    build:
      context: ./tools/robust_web_scraper
    x-google-run-job:
      tasks: 1
      timeout: 3600s
  play-publisher-tool:
    build:
      context: ./tools/google_play_publisher

================================================================================

--- File: backend/tools/google_play_publisher/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/google_play_publisher/main.py ---

import os
print("Google Play Publisher Tool job started.")
# Job logic will go here
print("Google Play Publisher Tool job finished.")

================================================================================

--- File: backend/tools/google_play_publisher/requirements.txt ---

# Placeholder for google-api-python-client, etc.

================================================================================

--- File: backend/tools/robust_web_scraper/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/robust_web_scraper/main.py ---

import os
print("Robust Web Scraper Tool job started.")
# Job logic will go here
print("Robust Web Scraper Tool job finished.")

================================================================================

--- File: backend/tools/robust_web_scraper/requirements.txt ---

# Placeholder for scraping libraries like playwright, beautifulsoup4, etc.

================================================================================

--- File: backend/services/discovery_cycle/Dockerfile ---

# Use an official lightweight Python image.
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /usr/src/app

# Copy the requirements file into the container
COPY requirements.txt ./

# Install the dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the main application file into the container
COPY main.py .

# Command to run the application using Gunicorn
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/discovery_cycle/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "Discovery Cycle Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/discovery_cycle/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/cso_vetting/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cso_vetting/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "CSO Vetting Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cso_vetting/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/ai_developer/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/ai_developer/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "AI Developer Agent Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/ai_developer/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/cpo_analysis/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cpo_analysis/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "CPO Analysis Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cpo_analysis/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/cmo_publishing/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cmo_publishing/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "CMO Publishing Agent is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cmo_publishing/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================



================================================================================

--- File: holistic_context-20250718-132044.txt ---

Project Context for app-factory-v2
Generated on: 2025-07-18T13:20:44.029019
================================================================================

--- Google Cloud Build Log (Latest) ---

No Google Cloud Build logs found or gcloud CLI is not configured.


================================================================================

--- File: .gitignore ---

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars

# Mac
.DS_Store

# Secrets
.env

================================================================================

--- File: README.md ---

# App Factory V2 - Cloud Native

This repository contains the complete infrastructure-as-code and application source for the App Factory V2 project, designed to run entirely on the Google Cloud Platform.

## Overview

The App Factory is an automated system that discovers, vets, develops, and publishes mobile applications. It operates as a collection of containerized microservices, each acting as a specialized "AI employee" within a digital assembly line. This project leverages Docker, Cloud Run, Cloud Build, and other GCP services to create a scalable and autonomous application production pipeline.

### Architecture

- **CI/CD:** Managed by Cloud Build, triggered by pushes to the `main` branch.
- **Orchestration:** Cloud Run services are defined and deployed via a `compose.yaml` file.
- **Services:** Each service (`discovery-cycle`, `cso-vetting`, etc.) runs as an independent Cloud Run service with a dedicated IAM service account.
- **Tools:** Long-running or heavy tasks (`robust-web-scraper`, `google-play-publisher`) are designed to run as Cloud Run Jobs.
- **Security:** Secrets are managed via Google Secret Manager. Service-to-service communication will be secured through IAM and service account identities.

================================================================================

--- File: cloudbuild.yaml ---

steps:
# Step 1: Build all container images defined in compose.yaml
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'build']
  dir: 'backend'

# Step 2: Push the built images to Google Artifact Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'push']
  dir: 'backend'

# Step 3: Deploy all services and jobs to Cloud Run using the compose file
- name: 'gcr.io/google.com/cloudsdktool/google-cloud-cli'
  entrypoint: 'gcloud'
  args:
    - 'run'
    - 'compose'
    - 'up'
    # No --file argument is needed when 'dir' is set correctly
  dir: 'backend'

options:
  logging: CLOUD_LOGGING_ONLY

================================================================================

--- File: holistic_context-20250718-120410.txt ---

Project Context for app-factory-v2
Generated on: 2025-07-18T12:04:10.547855
================================================================================

--- Google Cloud Build Log (Latest) ---

No Google Cloud Build logs found or gcloud CLI is not configured.


================================================================================

--- File: cloudbuild.yaml ---

steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'build']
  dir: 'backend'
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'push']
  dir: 'backend'
- name: 'google/cloud-sdk:latest'
  entrypoint: 'gcloud'
  args:
    - 'alpha'
    - 'run'
    - 'compose'
    - 'up'
    - '--file=compose.yaml'
    - '--project=app-factory-v2'
    - '--region=us-central1'
  dir: 'backend'
options:
  logging: CLOUD_LOGGING_ONLY

================================================================================

--- File: backend/.env ---



================================================================================

--- File: backend/.gitignore ---

.env


================================================================================

--- File: backend/compose.yaml ---

services:
  mcp-gateway:
    image: docker/mcp-gateway:latest
    ports:
      - "8811:8811"
    command:
      - "--transport=sse"
      - "--servers=github-tool,web-scraper-tool,play-publisher-tool"
  discovery-cycle-service:
    build:
      context: ./services/discovery_cycle
  cso-vetting-service:
    build:
      context: ./services/cso_vetting
  cpo-analysis-service:
    build:
      context: ./services/cpo_analysis
  ai-developer-agent-service:
    build:
      context: ./services/ai_developer
    x-google-secrets:
      - secret: gemini-api-key
        env: GEMINI_API_KEY
  cmo-publishing-agent:
    build:
      context: ./services/cmo_publishing
    x-google-secrets:
      - secret: google-play-api-key
        env: GOOGLE_PLAY_API_KEY
  github-tool:
    image: ghcr.io/github/github-mcp-server:latest
    x-google-secrets:
      - secret: github-token
        env: GITHUB_TOKEN
  web-scraper-tool:
    build:
      context: ./tools/robust_web_scraper
    x-google-run-job:
      tasks: 1
      timeout: 3600s
  play-publisher-tool:
    build:
      context: ./tools/google_play_publisher

================================================================================

--- File: backend/tools/google_play_publisher/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/google_play_publisher/main.py ---

import os
print("Google Play Publisher Tool job started.")
# Job logic will go here
print("Google Play Publisher Tool job finished.")

================================================================================

--- File: backend/tools/google_play_publisher/requirements.txt ---

# Placeholder for google-api-python-client, etc.

================================================================================

--- File: backend/tools/robust_web_scraper/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/robust_web_scraper/main.py ---

import os
print("Robust Web Scraper Tool job started.")
# Job logic will go here
print("Robust Web Scraper Tool job finished.")

================================================================================

--- File: backend/tools/robust_web_scraper/requirements.txt ---

# Placeholder for scraping libraries like playwright, beautifulsoup4, etc.

================================================================================

--- File: backend/services/discovery_cycle/Dockerfile ---

# Use an official lightweight Python image.
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /usr/src/app

# Copy the requirements file into the container
COPY requirements.txt ./

# Install the dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the main application file into the container
COPY main.py .

# Command to run the application using Gunicorn
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/discovery_cycle/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "Discovery Cycle Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/discovery_cycle/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/cso_vetting/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cso_vetting/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "CSO Vetting Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cso_vetting/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/ai_developer/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/ai_developer/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "AI Developer Agent Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/ai_developer/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/cpo_analysis/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cpo_analysis/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "CPO Analysis Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cpo_analysis/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/cmo_publishing/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cmo_publishing/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "CMO Publishing Agent is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cmo_publishing/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================



================================================================================

--- File: backend/.env ---



================================================================================

--- File: backend/.env.example ---

# This is an example file. For local development, copy this to .env and fill in the values.
# These are automatically used by Docker Compose for local runs. They are NOT used in GCP.

# The GCP Project ID where the services are running.
GCP_PROJECT="app-factory-v2"

================================================================================

--- File: backend/.gitignore ---

.env


================================================================================

--- File: backend/compose.yaml ---

services:
  mcp-gateway:
    image: docker/mcp-gateway:latest
    ports:
      - "8811:8811"
    command:
      - "--transport=sse"
      - "--servers=github-tool,web-scraper-tool,play-publisher-tool"
  discovery-cycle-service:
    build:
      context: ./services/discovery_cycle
    x-google-run-service:
      service-account: discovery-cycle-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
  cso-vetting-service:
    build:
      context: ./services/cso_vetting
    x-google-run-service:
      service-account: cso-vetting-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
  cpo-analysis-service:
    build:
      context: ./services/cpo_analysis
    x-google-run-service:
      service-account: cpo-analysis-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
  ai-developer-agent-service:
    build:
      context: ./services/ai_developer
    x-google-run-service:
      service-account: ai-developer-agent-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
    x-google-secrets:
      - secret: gemini-api-key
        env: GEMINI_API_KEY
  cmo-publishing-agent:
    build:
      context: ./services/cmo_publishing
    x-google-run-service:
      service-account: cmo-publishing-agent-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
    x-google-secrets:
      - secret: google-play-api-key
        env: GOOGLE_PLAY_API_KEY
  github-tool:
    image: ghcr.io/github/github-mcp-server:latest
    x-google-secrets:
      - secret: github-token
        env: GITHUB_TOKEN
  web-scraper-tool:
    build:
      context: ./tools/robust_web_scraper
    x-google-run-job:
      tasks: 1
      timeout: 3600s
  play-publisher-tool:
    build:
      context: ./tools/google_play_publisher
    x-google-run-job:
      tasks: 1
      timeout: 3600s

================================================================================

--- File: backend/tools/google_play_publisher/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/tools/google_play_publisher/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/google_play_publisher/main.py ---

from shared.utils import get_logger
from shared.gcp_client import get_secret

logger = get_logger(__name__)

def main():
    logger.info("Google Play Publisher Tool job started.")
    # This would be triggered as a Cloud Run Job
    
    # Placeholder logic
    play_api_key = get_secret("google-play-api-key")
    if not play_api_key:
        logger.error("Failed to get Google Play API key. Exiting.")
        return

    logger.info("Publishing to Google Play Store...")
    # ... actual publishing logic here ...
    logger.info("Google Play Publisher Tool job finished.")

if __name__ == "__main__":
    main()

================================================================================

--- File: backend/tools/google_play_publisher/requirements.txt ---

google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/tools/robust_web_scraper/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/tools/robust_web_scraper/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/robust_web_scraper/main.py ---

from shared.utils import get_logger

logger = get_logger(__name__)

def main():
    logger.info("Robust Web Scraper Tool job started.")
    # This would be triggered as a Cloud Run Job
    
    # Placeholder logic for scraping
    logger.info("Scraping websites for app ideas...")
    # ... actual scraping logic here ...
    logger.info("Robust Web Scraper Tool job finished.")

if __name__ == "__main__":
    main()

================================================================================

--- File: backend/tools/robust_web_scraper/requirements.txt ---

google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/shared/__init__.py ---

# This file makes the 'shared' directory a Python package.

================================================================================

--- File: backend/shared/gcp_client.py ---

import os
import google.auth.transport.requests
import google.oauth2.id_token
from google.cloud import firestore
from google.cloud import secretmanager
import requests

# Initialize clients globally to reuse connections
project_id = os.environ.get("GCP_PROJECT", "app-factory-v2")
db = firestore.Client(project=project_id)
secret_client = secretmanager.SecretManagerServiceClient()

def get_secret(secret_id: str, version_id: str = "latest") -> str:
    """Retrieves a secret value from Google Secret Manager."""
    name = f"projects/{project_id}/secrets/{secret_id}/versions/{version_id}"
    try:
        response = secret_client.access_secret_version(request={"name": name})
        return response.payload.data.decode("UTF-8")
    except Exception as e:
        print(f"Error accessing secret {secret_id}: {e}")
        return ""

def save_to_firestore(collection: str, doc_id: str, data: dict):
    """Saves or updates a document in a Firestore collection."""
    try:
        doc_ref = db.collection(collection).document(doc_id)
        doc_ref.set(data, merge=True)
        print(f"Successfully saved doc '{doc_id}' to collection '{collection}'.")
    except Exception as e:
        print(f"Error saving to Firestore: {e}")
        raise

def get_from_firestore(collection: str, doc_id: str) -> dict | None:
    """Retrieves a document from a Firestore collection."""
    try:
        doc_ref = db.collection(collection).document(doc_id)
        doc = doc_ref.get()
        if doc.exists:
            return doc.to_dict()
        else:
            print(f"Document '{doc_id}' not found in collection '{collection}'.")
            return None
    except Exception as e:
        print(f"Error retrieving from Firestore: {e}")
        raise

def make_internal_request(service_url: str, method: str = 'GET', data: dict = None) -> requests.Response:
    """
    Makes an authenticated HTTP request to another internal Cloud Run service.
    """
    try:
        auth_req = google.auth.transport.requests.Request()
        id_token = google.oauth2.id_token.fetch_id_token(auth_req, service_url)

        headers = {
            'Authorization': f'Bearer {id_token}',
            'Content-Type': 'application/json'
        }

        if method.upper() == 'POST':
            response = requests.post(service_url, headers=headers, json=data, timeout=30)
        else:
            response = requests.get(service_url, headers=headers, timeout=30)
        
        response.raise_for_status() # Raise an exception for bad status codes
        return response
        
    except Exception as e:
        print(f"Error making internal request to {service_url}: {e}")
        raise

================================================================================

--- File: backend/shared/utils.py ---

import logging
import sys

def get_logger(name: str):
    """
    Configures and returns a standardized logger.
    """
    logger = logging.getLogger(name)
    if not logger.handlers:
        logger.setLevel(logging.INFO)
        handler = logging.StreamHandler(sys.stdout)
        formatter = logging.Formatter(
            '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
        )
        handler.setFormatter(formatter)
        logger.addHandler(handler)
    return logger

================================================================================

--- File: backend/services/discovery_cycle/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/discovery_cycle/Dockerfile ---

# Use an official lightweight Python image.
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /usr/src/app

# Copy the requirements file into the container
COPY requirements.txt ./

# Install the dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the main application file into the container
COPY main.py .

# Command to run the application using Gunicorn
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/discovery_cycle/main.py ---

import os
import uuid
from flask import Flask, request, jsonify
from shared.utils import get_logger
from shared.gcp_client import save_to_firestore, make_internal_request

app = Flask(__name__)
logger = get_logger(__name__)

# The URL for the CSO Vetting Service will be automatically provided by Cloud Run
CSO_VETTING_SERVICE_URL = os.environ.get("CSO_VETTING_SERVICE_URL")

@app.route('/start', methods=['POST'])
def start_cycle():
    """
    Starts a new app discovery cycle.
    1. Creates a new "app_idea" document in Firestore.
    2. Triggers the CSO Vetting Service to begin its process.
    """
    logger.info("Discovery Cycle Service received a request to start.")

    # 1. Generate a new App Idea and save it to Firestore
    idea_id = f"idea-{uuid.uuid4()}"
    app_idea_data = {
        "idea_id": idea_id,
        "status": "PENDING_VETTING",
        "description": "A revolutionary new mobile app for discovering local coffee shops."
        # In a real scenario, this data would come from the web-scraper-tool
    }

    try:
        save_to_firestore("app_ideas", idea_id, app_idea_data)
    except Exception as e:
        logger.error(f"Failed to save new app idea '{idea_id}' to Firestore: {e}")
        return jsonify({"status": "error", "message": "Could not save app idea."}), 500

    # 2. Trigger the CSO Vetting Service
    if not CSO_VETTING_SERVICE_URL:
        logger.error("CSO_VETTING_SERVICE_URL environment variable is not set.")
        return jsonify({"status": "error", "message": "Downstream service is not configured."}), 500

    logger.info(f"Triggering CSO Vetting Service for idea '{idea_id}' at {CSO_VETTING_SERVICE_URL}")

    try:
        vetting_request_data = {"idea_id": idea_id}
        # This is a secure, authenticated call from one Cloud Run service to another
        response = make_internal_request(
            service_url=f"{CSO_VETTING_SERVICE_URL}/vet", 
            method='POST', 
            data=vetting_request_data
        )
        logger.info(f"CSO Vetting Service responded with status {response.status_code}: {response.text}")
    except Exception as e:
        logger.error(f"Failed to trigger CSO Vetting Service for idea '{idea_id}': {e}")
        # Here you might add logic to update the Firestore status to "VETTING_FAILED"
        return jsonify({"status": "error", "message": "Failed to trigger downstream service."}), 500

    return jsonify({
        "status": "success",
        "message": f"Discovery cycle started. App idea '{idea_id}' is now pending security vetting."
    })

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/discovery_cycle/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/cso_vetting/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/cso_vetting/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cso_vetting/main.py ---

import os
from flask import Flask, request, jsonify
from shared.utils import get_logger
from shared.gcp_client import get_from_firestore, save_to_firestore

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/vet', methods=['POST'])
def vet_idea():
    """
    Receives an app idea ID, retrieves it from Firestore, and performs a mock vetting.
    """
    logger.info("CSO Vetting Service received a request.")
    data = request.get_json()

    if not data or "idea_id" not in data:
        logger.warning("Request missing 'idea_id'.")
        return jsonify({"status": "error", "message": "Missing 'idea_id' in request body."}), 400

    idea_id = data["idea_id"]
    logger.info(f"Starting security vetting for app idea: {idea_id}")

    try:
        # 1. Get the idea from Firestore
        app_idea = get_from_firestore("app_ideas", idea_id)
        if not app_idea:
            return jsonify({"status": "error", "message": f"App idea '{idea_id}' not found."}), 404

        # 2. Perform mock vetting logic
        logger.info(f"Performing security checks on: {app_idea.get('description')}")
        # ... In a real scenario, complex security checks would happen here ...
        
        # 3. Update the status in Firestore
        updated_data = {"status": "VETTING_PASSED"}
        save_to_firestore("app_ideas", idea_id, updated_data)

        logger.info(f"Security vetting for '{idea_id}' passed.")
        return jsonify({"status": "success", "message": f"Vetting passed for '{idea_id}'."})

    except Exception as e:
        logger.error(f"An error occurred during vetting for '{idea_id}': {e}")
        # Optionally, update Firestore status to "VETTING_FAILED"
        return jsonify({"status": "error", "message": "An internal error occurred."}), 500

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cso_vetting/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/ai_developer/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/ai_developer/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/ai_developer/main.py ---

import os
from flask import Flask, jsonify
from shared.utils import get_logger
from shared.gcp_client import get_secret

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/')
def handle_request():
    logger.info("AI Developer Agent Service received a request.")
    # Example of using a secret
    api_key = get_secret("gemini-api-key")
    if not api_key:
        logger.error("Could not retrieve Gemini API key.")
        return jsonify({"status": "error", "message": "Missing secret"}), 500

    # Placeholder for development logic
    logger.info("Development logic would run here.")
    return jsonify({"status": "success", "message": "AI Developer task placeholder complete."})

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/ai_developer/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/cpo_analysis/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/cpo_analysis/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cpo_analysis/main.py ---

import os
from flask import Flask, jsonify
from shared.utils import get_logger

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/')
def handle_request():
    logger.info("CPO Analysis Service received a request.")
    # Placeholder for analysis logic
    logger.info("Product analysis logic would run here.")
    return jsonify({"status": "success", "message": "CPO Analysis task placeholder complete."})

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cpo_analysis/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/cmo_publishing/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/cmo_publishing/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cmo_publishing/main.py ---

import os
from flask import Flask, jsonify
from shared.utils import get_logger
from shared.gcp_client import get_secret

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/')
def handle_request():
    logger.info("CMO Publishing Agent received a request.")
    # Example of using a secret
    api_key = get_secret("google-play-api-key")
    if not api_key:
        logger.error("Could not retrieve Google Play API key.")
        return jsonify({"status": "error", "message": "Missing secret"}), 500

    # Placeholder for publishing logic
    logger.info("Publishing logic would run here.")
    return jsonify({"status": "success", "message": "CMO Publishing task placeholder complete."})

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cmo_publishing/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================



================================================================================

--- File: holistic_context-20250718-234542.txt ---

Project Context for app-factory-v2
Generated on: 2025-07-18T23:45:42.509136
================================================================================

--- Google Cloud Build Log (Latest) ---

No Google Cloud Build logs found or gcloud CLI is not configured.


================================================================================

--- File: .gitignore ---

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars

# Mac
.DS_Store

# Secrets
.env

================================================================================

--- File: README.md ---

# App Factory V2 - Cloud Native

This repository contains the complete infrastructure-as-code and application source for the App Factory V2 project, designed to run entirely on the Google Cloud Platform.

## Overview

The App Factory is an automated system that discovers, vets, develops, and publishes mobile applications. It operates as a collection of containerized microservices, each acting as a specialized "AI employee" within a digital assembly line. This project leverages Docker, Cloud Run, Cloud Build, and other GCP services to create a scalable and autonomous application production pipeline.

### Architecture

- **CI/CD:** Managed by Cloud Build, triggered by pushes to the `main` branch.
- **Orchestration:** Cloud Run services are defined and deployed via a `compose.yaml` file.
- **Services:** Each service (`discovery-cycle`, `cso-vetting`, etc.) runs as an independent Cloud Run service with a dedicated IAM service account.
- **Tools:** Long-running or heavy tasks (`robust-web-scraper`, `google-play-publisher`) are designed to run as Cloud Run Jobs.
- **Security:** Secrets are managed via Google Secret Manager. Service-to-service communication will be secured through IAM and service account identities.

================================================================================

--- File: cloudbuild.yaml ---

steps:
# Step 1: Build all container images locally in the build worker.
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'build']
  dir: 'backend'

# Step 2: Push the explicitly named images to Artifact Registry.
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'push']
  dir: 'backend'

# Step 3: Deploy the now-existing images from the registry to Cloud Run.
- name: 'gcr.io/google.com/cloudsdktool/google-cloud-cli'
  entrypoint: 'gcloud'
  args:
    - 'alpha'
    - 'run'
    - 'compose'
    - 'up'
    - '--region=us-central1'
  dir: 'backend'

options:
  logging: CLOUD_LOGGING_ONLY

================================================================================

--- File: holistic_context-20250718-120410.txt ---

Project Context for app-factory-v2
Generated on: 2025-07-18T12:04:10.547855
================================================================================

--- Google Cloud Build Log (Latest) ---

No Google Cloud Build logs found or gcloud CLI is not configured.


================================================================================

--- File: cloudbuild.yaml ---

steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'build']
  dir: 'backend'
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'push']
  dir: 'backend'
- name: 'google/cloud-sdk:latest'
  entrypoint: 'gcloud'
  args:
    - 'alpha'
    - 'run'
    - 'compose'
    - 'up'
    - '--file=compose.yaml'
    - '--project=app-factory-v2'
    - '--region=us-central1'
  dir: 'backend'
options:
  logging: CLOUD_LOGGING_ONLY

================================================================================

--- File: backend/.env ---



================================================================================

--- File: backend/.gitignore ---

.env


================================================================================

--- File: backend/compose.yaml ---

services:
  mcp-gateway:
    image: docker/mcp-gateway:latest
    ports:
      - "8811:8811"
    command:
      - "--transport=sse"
      - "--servers=github-tool,web-scraper-tool,play-publisher-tool"
  discovery-cycle-service:
    build:
      context: ./services/discovery_cycle
  cso-vetting-service:
    build:
      context: ./services/cso_vetting
  cpo-analysis-service:
    build:
      context: ./services/cpo_analysis
  ai-developer-agent-service:
    build:
      context: ./services/ai_developer
    x-google-secrets:
      - secret: gemini-api-key
        env: GEMINI_API_KEY
  cmo-publishing-agent:
    build:
      context: ./services/cmo_publishing
    x-google-secrets:
      - secret: google-play-api-key
        env: GOOGLE_PLAY_API_KEY
  github-tool:
    image: ghcr.io/github/github-mcp-server:latest
    x-google-secrets:
      - secret: github-token
        env: GITHUB_TOKEN
  web-scraper-tool:
    build:
      context: ./tools/robust_web_scraper
    x-google-run-job:
      tasks: 1
      timeout: 3600s
  play-publisher-tool:
    build:
      context: ./tools/google_play_publisher

================================================================================

--- File: backend/tools/google_play_publisher/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/google_play_publisher/main.py ---

import os
print("Google Play Publisher Tool job started.")
# Job logic will go here
print("Google Play Publisher Tool job finished.")

================================================================================

--- File: backend/tools/google_play_publisher/requirements.txt ---

# Placeholder for google-api-python-client, etc.

================================================================================

--- File: backend/tools/robust_web_scraper/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/robust_web_scraper/main.py ---

import os
print("Robust Web Scraper Tool job started.")
# Job logic will go here
print("Robust Web Scraper Tool job finished.")

================================================================================

--- File: backend/tools/robust_web_scraper/requirements.txt ---

# Placeholder for scraping libraries like playwright, beautifulsoup4, etc.

================================================================================

--- File: backend/services/discovery_cycle/Dockerfile ---

# Use an official lightweight Python image.
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /usr/src/app

# Copy the requirements file into the container
COPY requirements.txt ./

# Install the dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the main application file into the container
COPY main.py .

# Command to run the application using Gunicorn
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/discovery_cycle/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "Discovery Cycle Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/discovery_cycle/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/cso_vetting/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cso_vetting/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "CSO Vetting Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cso_vetting/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/ai_developer/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/ai_developer/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "AI Developer Agent Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/ai_developer/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/cpo_analysis/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cpo_analysis/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "CPO Analysis Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cpo_analysis/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/cmo_publishing/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cmo_publishing/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "CMO Publishing Agent is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cmo_publishing/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================



================================================================================

--- File: holistic_context-20250718-132044.txt ---

Project Context for app-factory-v2
Generated on: 2025-07-18T13:20:44.029019
================================================================================

--- Google Cloud Build Log (Latest) ---

No Google Cloud Build logs found or gcloud CLI is not configured.


================================================================================

--- File: .gitignore ---

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars

# Mac
.DS_Store

# Secrets
.env

================================================================================

--- File: README.md ---

# App Factory V2 - Cloud Native

This repository contains the complete infrastructure-as-code and application source for the App Factory V2 project, designed to run entirely on the Google Cloud Platform.

## Overview

The App Factory is an automated system that discovers, vets, develops, and publishes mobile applications. It operates as a collection of containerized microservices, each acting as a specialized "AI employee" within a digital assembly line. This project leverages Docker, Cloud Run, Cloud Build, and other GCP services to create a scalable and autonomous application production pipeline.

### Architecture

- **CI/CD:** Managed by Cloud Build, triggered by pushes to the `main` branch.
- **Orchestration:** Cloud Run services are defined and deployed via a `compose.yaml` file.
- **Services:** Each service (`discovery-cycle`, `cso-vetting`, etc.) runs as an independent Cloud Run service with a dedicated IAM service account.
- **Tools:** Long-running or heavy tasks (`robust-web-scraper`, `google-play-publisher`) are designed to run as Cloud Run Jobs.
- **Security:** Secrets are managed via Google Secret Manager. Service-to-service communication will be secured through IAM and service account identities.

================================================================================

--- File: cloudbuild.yaml ---

steps:
# Step 1: Build all container images defined in compose.yaml
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'build']
  dir: 'backend'

# Step 2: Push the built images to Google Artifact Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'push']
  dir: 'backend'

# Step 3: Deploy all services and jobs to Cloud Run using the compose file
- name: 'gcr.io/google.com/cloudsdktool/google-cloud-cli'
  entrypoint: 'gcloud'
  args:
    - 'run'
    - 'compose'
    - 'up'
    # No --file argument is needed when 'dir' is set correctly
  dir: 'backend'

options:
  logging: CLOUD_LOGGING_ONLY

================================================================================

--- File: holistic_context-20250718-120410.txt ---

Project Context for app-factory-v2
Generated on: 2025-07-18T12:04:10.547855
================================================================================

--- Google Cloud Build Log (Latest) ---

No Google Cloud Build logs found or gcloud CLI is not configured.


================================================================================

--- File: cloudbuild.yaml ---

steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'build']
  dir: 'backend'
- name: 'gcr.io/cloud-builders/docker'
  args: ['compose', '-f', 'compose.yaml', 'push']
  dir: 'backend'
- name: 'google/cloud-sdk:latest'
  entrypoint: 'gcloud'
  args:
    - 'alpha'
    - 'run'
    - 'compose'
    - 'up'
    - '--file=compose.yaml'
    - '--project=app-factory-v2'
    - '--region=us-central1'
  dir: 'backend'
options:
  logging: CLOUD_LOGGING_ONLY

================================================================================

--- File: backend/.env ---



================================================================================

--- File: backend/.gitignore ---

.env


================================================================================

--- File: backend/compose.yaml ---

services:
  mcp-gateway:
    image: docker/mcp-gateway:latest
    ports:
      - "8811:8811"
    command:
      - "--transport=sse"
      - "--servers=github-tool,web-scraper-tool,play-publisher-tool"
  discovery-cycle-service:
    build:
      context: ./services/discovery_cycle
  cso-vetting-service:
    build:
      context: ./services/cso_vetting
  cpo-analysis-service:
    build:
      context: ./services/cpo_analysis
  ai-developer-agent-service:
    build:
      context: ./services/ai_developer
    x-google-secrets:
      - secret: gemini-api-key
        env: GEMINI_API_KEY
  cmo-publishing-agent:
    build:
      context: ./services/cmo_publishing
    x-google-secrets:
      - secret: google-play-api-key
        env: GOOGLE_PLAY_API_KEY
  github-tool:
    image: ghcr.io/github/github-mcp-server:latest
    x-google-secrets:
      - secret: github-token
        env: GITHUB_TOKEN
  web-scraper-tool:
    build:
      context: ./tools/robust_web_scraper
    x-google-run-job:
      tasks: 1
      timeout: 3600s
  play-publisher-tool:
    build:
      context: ./tools/google_play_publisher

================================================================================

--- File: backend/tools/google_play_publisher/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/google_play_publisher/main.py ---

import os
print("Google Play Publisher Tool job started.")
# Job logic will go here
print("Google Play Publisher Tool job finished.")

================================================================================

--- File: backend/tools/google_play_publisher/requirements.txt ---

# Placeholder for google-api-python-client, etc.

================================================================================

--- File: backend/tools/robust_web_scraper/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/robust_web_scraper/main.py ---

import os
print("Robust Web Scraper Tool job started.")
# Job logic will go here
print("Robust Web Scraper Tool job finished.")

================================================================================

--- File: backend/tools/robust_web_scraper/requirements.txt ---

# Placeholder for scraping libraries like playwright, beautifulsoup4, etc.

================================================================================

--- File: backend/services/discovery_cycle/Dockerfile ---

# Use an official lightweight Python image.
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /usr/src/app

# Copy the requirements file into the container
COPY requirements.txt ./

# Install the dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the main application file into the container
COPY main.py .

# Command to run the application using Gunicorn
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/discovery_cycle/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "Discovery Cycle Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/discovery_cycle/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/cso_vetting/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cso_vetting/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "CSO Vetting Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cso_vetting/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/ai_developer/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/ai_developer/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "AI Developer Agent Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/ai_developer/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/cpo_analysis/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cpo_analysis/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "CPO Analysis Service is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cpo_analysis/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================

--- File: backend/services/cmo_publishing/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cmo_publishing/main.py ---

from flask import Flask
import os
app = Flask(__name__)
@app.route('/')
def hello():
    return "CMO Publishing Agent is alive!"
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cmo_publishing/requirements.txt ---

Flask==2.2.2
gunicorn==20.1.0

================================================================================



================================================================================

--- File: backend/.env ---



================================================================================

--- File: backend/.env.example ---

# This is an example file. For local development, copy this to .env and fill in the values.
# These are automatically used by Docker Compose for local runs. They are NOT used in GCP.

# The GCP Project ID where the services are running.
GCP_PROJECT="app-factory-v2"

================================================================================

--- File: backend/.gitignore ---

.env


================================================================================

--- File: backend/compose.yaml ---

services:
  mcp-gateway:
    image: docker/mcp-gateway:latest
    ports:
      - "8811:8811"
    command:
      - "--transport=sse"
      - "--servers=github-tool,web-scraper-tool,play-publisher-tool"
  discovery-cycle-service:
    build:
      context: ./services/discovery_cycle
    x-google-run-service:
      service-account: discovery-cycle-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
  cso-vetting-service:
    build:
      context: ./services/cso_vetting
    x-google-run-service:
      service-account: cso-vetting-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
  cpo-analysis-service:
    build:
      context: ./services/cpo_analysis
    x-google-run-service:
      service-account: cpo-analysis-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
  ai-developer-agent-service:
    build:
      context: ./services/ai_developer
    x-google-run-service:
      service-account: ai-developer-agent-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
    x-google-secrets:
      - secret: gemini-api-key
        env: GEMINI_API_KEY
  cmo-publishing-agent:
    build:
      context: ./services/cmo_publishing
    x-google-run-service:
      service-account: cmo-publishing-agent-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
    x-google-secrets:
      - secret: google-play-api-key
        env: GOOGLE_PLAY_API_KEY
  github-tool:
    image: ghcr.io/github/github-mcp-server:latest
    x-google-secrets:
      - secret: github-token
        env: GITHUB_TOKEN
  web-scraper-tool:
    build:
      context: ./tools/robust_web_scraper
    x-google-run-job:
      tasks: 1
      timeout: 3600s
  play-publisher-tool:
    build:
      context: ./tools/google_play_publisher
    x-google-run-job:
      tasks: 1
      timeout: 3600s

================================================================================

--- File: backend/tools/google_play_publisher/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/tools/google_play_publisher/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/google_play_publisher/main.py ---

from shared.utils import get_logger
from shared.gcp_client import get_secret

logger = get_logger(__name__)

def main():
    logger.info("Google Play Publisher Tool job started.")
    # This would be triggered as a Cloud Run Job
    
    # Placeholder logic
    play_api_key = get_secret("google-play-api-key")
    if not play_api_key:
        logger.error("Failed to get Google Play API key. Exiting.")
        return

    logger.info("Publishing to Google Play Store...")
    # ... actual publishing logic here ...
    logger.info("Google Play Publisher Tool job finished.")

if __name__ == "__main__":
    main()

================================================================================

--- File: backend/tools/google_play_publisher/requirements.txt ---

google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/tools/robust_web_scraper/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/tools/robust_web_scraper/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/robust_web_scraper/main.py ---

from shared.utils import get_logger

logger = get_logger(__name__)

def main():
    logger.info("Robust Web Scraper Tool job started.")
    # This would be triggered as a Cloud Run Job
    
    # Placeholder logic for scraping
    logger.info("Scraping websites for app ideas...")
    # ... actual scraping logic here ...
    logger.info("Robust Web Scraper Tool job finished.")

if __name__ == "__main__":
    main()

================================================================================

--- File: backend/tools/robust_web_scraper/requirements.txt ---

google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/shared/__init__.py ---

# This file makes the 'shared' directory a Python package.

================================================================================

--- File: backend/shared/gcp_client.py ---

import os
import google.auth.transport.requests
import google.oauth2.id_token
from google.cloud import firestore
from google.cloud import secretmanager
import requests

# Initialize clients globally to reuse connections
project_id = os.environ.get("GCP_PROJECT", "app-factory-v2")
db = firestore.Client(project=project_id)
secret_client = secretmanager.SecretManagerServiceClient()

def get_secret(secret_id: str, version_id: str = "latest") -> str:
    """Retrieves a secret value from Google Secret Manager."""
    name = f"projects/{project_id}/secrets/{secret_id}/versions/{version_id}"
    try:
        response = secret_client.access_secret_version(request={"name": name})
        return response.payload.data.decode("UTF-8")
    except Exception as e:
        print(f"Error accessing secret {secret_id}: {e}")
        return ""

def save_to_firestore(collection: str, doc_id: str, data: dict):
    """Saves or updates a document in a Firestore collection."""
    try:
        doc_ref = db.collection(collection).document(doc_id)
        doc_ref.set(data, merge=True)
        print(f"Successfully saved doc '{doc_id}' to collection '{collection}'.")
    except Exception as e:
        print(f"Error saving to Firestore: {e}")
        raise

def get_from_firestore(collection: str, doc_id: str) -> dict | None:
    """Retrieves a document from a Firestore collection."""
    try:
        doc_ref = db.collection(collection).document(doc_id)
        doc = doc_ref.get()
        if doc.exists:
            return doc.to_dict()
        else:
            print(f"Document '{doc_id}' not found in collection '{collection}'.")
            return None
    except Exception as e:
        print(f"Error retrieving from Firestore: {e}")
        raise

def make_internal_request(service_url: str, method: str = 'GET', data: dict = None) -> requests.Response:
    """
    Makes an authenticated HTTP request to another internal Cloud Run service.
    """
    try:
        auth_req = google.auth.transport.requests.Request()
        id_token = google.oauth2.id_token.fetch_id_token(auth_req, service_url)

        headers = {
            'Authorization': f'Bearer {id_token}',
            'Content-Type': 'application/json'
        }

        if method.upper() == 'POST':
            response = requests.post(service_url, headers=headers, json=data, timeout=30)
        else:
            response = requests.get(service_url, headers=headers, timeout=30)
        
        response.raise_for_status() # Raise an exception for bad status codes
        return response
        
    except Exception as e:
        print(f"Error making internal request to {service_url}: {e}")
        raise

================================================================================

--- File: backend/shared/utils.py ---

import logging
import sys

def get_logger(name: str):
    """
    Configures and returns a standardized logger.
    """
    logger = logging.getLogger(name)
    if not logger.handlers:
        logger.setLevel(logging.INFO)
        handler = logging.StreamHandler(sys.stdout)
        formatter = logging.Formatter(
            '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
        )
        handler.setFormatter(formatter)
        logger.addHandler(handler)
    return logger

================================================================================

--- File: backend/services/discovery_cycle/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/discovery_cycle/Dockerfile ---

# Use an official lightweight Python image.
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /usr/src/app

# Copy the requirements file into the container
COPY requirements.txt ./

# Install the dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the main application file into the container
COPY main.py .

# Command to run the application using Gunicorn
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/discovery_cycle/main.py ---

import os
import uuid
from flask import Flask, request, jsonify
from shared.utils import get_logger
from shared.gcp_client import save_to_firestore, make_internal_request

app = Flask(__name__)
logger = get_logger(__name__)

# The URL for the CSO Vetting Service will be automatically provided by Cloud Run
CSO_VETTING_SERVICE_URL = os.environ.get("CSO_VETTING_SERVICE_URL")

@app.route('/start', methods=['POST'])
def start_cycle():
    """
    Starts a new app discovery cycle.
    1. Creates a new "app_idea" document in Firestore.
    2. Triggers the CSO Vetting Service to begin its process.
    """
    logger.info("Discovery Cycle Service received a request to start.")

    # 1. Generate a new App Idea and save it to Firestore
    idea_id = f"idea-{uuid.uuid4()}"
    app_idea_data = {
        "idea_id": idea_id,
        "status": "PENDING_VETTING",
        "description": "A revolutionary new mobile app for discovering local coffee shops."
        # In a real scenario, this data would come from the web-scraper-tool
    }

    try:
        save_to_firestore("app_ideas", idea_id, app_idea_data)
    except Exception as e:
        logger.error(f"Failed to save new app idea '{idea_id}' to Firestore: {e}")
        return jsonify({"status": "error", "message": "Could not save app idea."}), 500

    # 2. Trigger the CSO Vetting Service
    if not CSO_VETTING_SERVICE_URL:
        logger.error("CSO_VETTING_SERVICE_URL environment variable is not set.")
        return jsonify({"status": "error", "message": "Downstream service is not configured."}), 500

    logger.info(f"Triggering CSO Vetting Service for idea '{idea_id}' at {CSO_VETTING_SERVICE_URL}")

    try:
        vetting_request_data = {"idea_id": idea_id}
        # This is a secure, authenticated call from one Cloud Run service to another
        response = make_internal_request(
            service_url=f"{CSO_VETTING_SERVICE_URL}/vet", 
            method='POST', 
            data=vetting_request_data
        )
        logger.info(f"CSO Vetting Service responded with status {response.status_code}: {response.text}")
    except Exception as e:
        logger.error(f"Failed to trigger CSO Vetting Service for idea '{idea_id}': {e}")
        # Here you might add logic to update the Firestore status to "VETTING_FAILED"
        return jsonify({"status": "error", "message": "Failed to trigger downstream service."}), 500

    return jsonify({
        "status": "success",
        "message": f"Discovery cycle started. App idea '{idea_id}' is now pending security vetting."
    })

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/discovery_cycle/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/cso_vetting/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/cso_vetting/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cso_vetting/main.py ---

import os
from flask import Flask, request, jsonify
from shared.utils import get_logger
from shared.gcp_client import get_from_firestore, save_to_firestore

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/vet', methods=['POST'])
def vet_idea():
    """
    Receives an app idea ID, retrieves it from Firestore, and performs a mock vetting.
    """
    logger.info("CSO Vetting Service received a request.")
    data = request.get_json()

    if not data or "idea_id" not in data:
        logger.warning("Request missing 'idea_id'.")
        return jsonify({"status": "error", "message": "Missing 'idea_id' in request body."}), 400

    idea_id = data["idea_id"]
    logger.info(f"Starting security vetting for app idea: {idea_id}")

    try:
        # 1. Get the idea from Firestore
        app_idea = get_from_firestore("app_ideas", idea_id)
        if not app_idea:
            return jsonify({"status": "error", "message": f"App idea '{idea_id}' not found."}), 404

        # 2. Perform mock vetting logic
        logger.info(f"Performing security checks on: {app_idea.get('description')}")
        # ... In a real scenario, complex security checks would happen here ...
        
        # 3. Update the status in Firestore
        updated_data = {"status": "VETTING_PASSED"}
        save_to_firestore("app_ideas", idea_id, updated_data)

        logger.info(f"Security vetting for '{idea_id}' passed.")
        return jsonify({"status": "success", "message": f"Vetting passed for '{idea_id}'."})

    except Exception as e:
        logger.error(f"An error occurred during vetting for '{idea_id}': {e}")
        # Optionally, update Firestore status to "VETTING_FAILED"
        return jsonify({"status": "error", "message": "An internal error occurred."}), 500

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cso_vetting/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/ai_developer/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/ai_developer/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/ai_developer/main.py ---

import os
from flask import Flask, jsonify
from shared.utils import get_logger
from shared.gcp_client import get_secret

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/')
def handle_request():
    logger.info("AI Developer Agent Service received a request.")
    # Example of using a secret
    api_key = get_secret("gemini-api-key")
    if not api_key:
        logger.error("Could not retrieve Gemini API key.")
        return jsonify({"status": "error", "message": "Missing secret"}), 500

    # Placeholder for development logic
    logger.info("Development logic would run here.")
    return jsonify({"status": "success", "message": "AI Developer task placeholder complete."})

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/ai_developer/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/cpo_analysis/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/cpo_analysis/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cpo_analysis/main.py ---

import os
from flask import Flask, jsonify
from shared.utils import get_logger

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/')
def handle_request():
    logger.info("CPO Analysis Service received a request.")
    # Placeholder for analysis logic
    logger.info("Product analysis logic would run here.")
    return jsonify({"status": "success", "message": "CPO Analysis task placeholder complete."})

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cpo_analysis/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/cmo_publishing/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/cmo_publishing/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cmo_publishing/main.py ---

import os
from flask import Flask, jsonify
from shared.utils import get_logger
from shared.gcp_client import get_secret

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/')
def handle_request():
    logger.info("CMO Publishing Agent received a request.")
    # Example of using a secret
    api_key = get_secret("google-play-api-key")
    if not api_key:
        logger.error("Could not retrieve Google Play API key.")
        return jsonify({"status": "error", "message": "Missing secret"}), 500

    # Placeholder for publishing logic
    logger.info("Publishing logic would run here.")
    return jsonify({"status": "success", "message": "CMO Publishing task placeholder complete."})

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cmo_publishing/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================



================================================================================

--- File: backend/.env ---



================================================================================

--- File: backend/.env.example ---

# This is an example file. For local development, copy this to .env and fill in the values.
# These are automatically used by Docker Compose for local runs. They are NOT used in GCP.

# The GCP Project ID where the services are running.
GCP_PROJECT="app-factory-v2"

================================================================================

--- File: backend/.gitignore ---

.env


================================================================================

--- File: backend/compose.yaml ---

services:
  mcp-gateway:
    image: docker/mcp-gateway:latest
    ports:
      - "8811:8811"
    command:
      - "--transport=sse"
      - "--servers=github-tool,web-scraper-tool,play-publisher-tool"
  discovery-cycle-service:
    build:
      context: ./services/discovery_cycle
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/discovery-cycle-service
    x-google-run-service:
      service-account: discovery-cycle-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
  cso-vetting-service:
    build:
      context: ./services/cso_vetting
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/cso-vetting-service
    x-google-run-service:
      service-account: cso-vetting-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
  cpo-analysis-service:
    build:
      context: ./services/cpo_analysis
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/cpo-analysis-service
    x-google-run-service:
      service-account: cpo-analysis-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
  ai-developer-agent-service:
    build:
      context: ./services/ai_developer
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/ai-developer-agent-service
    x-google-run-service:
      service-account: ai-developer-agent-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
    x-google-secrets:
      - secret: gemini-api-key
        env: GEMINI_API_KEY
  cmo-publishing-agent:
    build:
      context: ./services/cmo_publishing
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/cmo-publishing-agent
    x-google-run-service:
      service-account: cmo-publishing-agent-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
    x-google-secrets:
      - secret: google-play-api-key
        env: GOOGLE_PLAY_API_KEY
  github-tool:
    image: ghcr.io/github/github-mcp-server:latest
    x-google-secrets:
      - secret: github-token
        env: GITHUB_TOKEN
  web-scraper-tool:
    build:
      context: ./tools/robust_web_scraper
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/web-scraper-tool
    x-google-run-job:
      tasks: 1
      timeout: 3600s
  play-publisher-tool:
    build:
      context: ./tools/google_play_publisher
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/play-publisher-tool
    x-google-run-job:
      tasks: 1
      timeout: 3600s

================================================================================

--- File: backend/tools/google_play_publisher/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/tools/google_play_publisher/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/google_play_publisher/main.py ---

from shared.utils import get_logger
from shared.gcp_client import get_secret

logger = get_logger(__name__)

def main():
    logger.info("Google Play Publisher Tool job started.")
    # This would be triggered as a Cloud Run Job
    
    # Placeholder logic
    play_api_key = get_secret("google-play-api-key")
    if not play_api_key:
        logger.error("Failed to get Google Play API key. Exiting.")
        return

    logger.info("Publishing to Google Play Store...")
    # ... actual publishing logic here ...
    logger.info("Google Play Publisher Tool job finished.")

if __name__ == "__main__":
    main()

================================================================================

--- File: backend/tools/google_play_publisher/requirements.txt ---

google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/tools/robust_web_scraper/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/tools/robust_web_scraper/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/robust_web_scraper/main.py ---

from shared.utils import get_logger

logger = get_logger(__name__)

def main():
    logger.info("Robust Web Scraper Tool job started.")
    # This would be triggered as a Cloud Run Job
    
    # Placeholder logic for scraping
    logger.info("Scraping websites for app ideas...")
    # ... actual scraping logic here ...
    logger.info("Robust Web Scraper Tool job finished.")

if __name__ == "__main__":
    main()

================================================================================

--- File: backend/tools/robust_web_scraper/requirements.txt ---

google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/shared/__init__.py ---

# This file makes the 'shared' directory a Python package.

================================================================================

--- File: backend/shared/gcp_client.py ---

import os
import google.auth.transport.requests
import google.oauth2.id_token
from google.cloud import firestore
from google.cloud import secretmanager
import requests

# Initialize clients globally to reuse connections
project_id = os.environ.get("GCP_PROJECT", "app-factory-v2")
db = firestore.Client(project=project_id)
secret_client = secretmanager.SecretManagerServiceClient()

def get_secret(secret_id: str, version_id: str = "latest") -> str:
    """Retrieves a secret value from Google Secret Manager."""
    name = f"projects/{project_id}/secrets/{secret_id}/versions/{version_id}"
    try:
        response = secret_client.access_secret_version(request={"name": name})
        return response.payload.data.decode("UTF-8")
    except Exception as e:
        print(f"Error accessing secret {secret_id}: {e}")
        return ""

def save_to_firestore(collection: str, doc_id: str, data: dict):
    """Saves or updates a document in a Firestore collection."""
    try:
        doc_ref = db.collection(collection).document(doc_id)
        doc_ref.set(data, merge=True)
        print(f"Successfully saved doc '{doc_id}' to collection '{collection}'.")
    except Exception as e:
        print(f"Error saving to Firestore: {e}")
        raise

def get_from_firestore(collection: str, doc_id: str) -> dict | None:
    """Retrieves a document from a Firestore collection."""
    try:
        doc_ref = db.collection(collection).document(doc_id)
        doc = doc_ref.get()
        if doc.exists:
            return doc.to_dict()
        else:
            print(f"Document '{doc_id}' not found in collection '{collection}'.")
            return None
    except Exception as e:
        print(f"Error retrieving from Firestore: {e}")
        raise

def make_internal_request(service_url: str, method: str = 'GET', data: dict = None) -> requests.Response:
    """
    Makes an authenticated HTTP request to another internal Cloud Run service.
    """
    try:
        auth_req = google.auth.transport.requests.Request()
        id_token = google.oauth2.id_token.fetch_id_token(auth_req, service_url)

        headers = {
            'Authorization': f'Bearer {id_token}',
            'Content-Type': 'application/json'
        }

        if method.upper() == 'POST':
            response = requests.post(service_url, headers=headers, json=data, timeout=30)
        else:
            response = requests.get(service_url, headers=headers, timeout=30)
        
        response.raise_for_status() # Raise an exception for bad status codes
        return response
        
    except Exception as e:
        print(f"Error making internal request to {service_url}: {e}")
        raise

================================================================================

--- File: backend/shared/utils.py ---

import logging
import sys

def get_logger(name: str):
    """
    Configures and returns a standardized logger.
    """
    logger = logging.getLogger(name)
    if not logger.handlers:
        logger.setLevel(logging.INFO)
        handler = logging.StreamHandler(sys.stdout)
        formatter = logging.Formatter(
            '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
        )
        handler.setFormatter(formatter)
        logger.addHandler(handler)
    return logger

================================================================================

--- File: backend/services/discovery_cycle/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/discovery_cycle/Dockerfile ---

# Use an official lightweight Python image.
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /usr/src/app

# Copy the requirements file into the container
COPY requirements.txt ./

# Install the dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the main application file into the container
COPY main.py .

# Command to run the application using Gunicorn
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/discovery_cycle/main.py ---

import os
import uuid
from flask import Flask, request, jsonify
from shared.utils import get_logger
from shared.gcp_client import save_to_firestore, make_internal_request

app = Flask(__name__)
logger = get_logger(__name__)

# The URL for the CSO Vetting Service will be automatically provided by Cloud Run
CSO_VETTING_SERVICE_URL = os.environ.get("CSO_VETTING_SERVICE_URL")

@app.route('/start', methods=['POST'])
def start_cycle():
    """
    Starts a new app discovery cycle.
    1. Creates a new "app_idea" document in Firestore.
    2. Triggers the CSO Vetting Service to begin its process.
    """
    logger.info("Discovery Cycle Service received a request to start.")

    # 1. Generate a new App Idea and save it to Firestore
    idea_id = f"idea-{uuid.uuid4()}"
    app_idea_data = {
        "idea_id": idea_id,
        "status": "PENDING_VETTING",
        "description": "A revolutionary new mobile app for discovering local coffee shops."
        # In a real scenario, this data would come from the web-scraper-tool
    }

    try:
        save_to_firestore("app_ideas", idea_id, app_idea_data)
    except Exception as e:
        logger.error(f"Failed to save new app idea '{idea_id}' to Firestore: {e}")
        return jsonify({"status": "error", "message": "Could not save app idea."}), 500

    # 2. Trigger the CSO Vetting Service
    if not CSO_VETTING_SERVICE_URL:
        logger.error("CSO_VETTING_SERVICE_URL environment variable is not set.")
        return jsonify({"status": "error", "message": "Downstream service is not configured."}), 500

    logger.info(f"Triggering CSO Vetting Service for idea '{idea_id}' at {CSO_VETTING_SERVICE_URL}")

    try:
        vetting_request_data = {"idea_id": idea_id}
        # This is a secure, authenticated call from one Cloud Run service to another
        response = make_internal_request(
            service_url=f"{CSO_VETTING_SERVICE_URL}/vet", 
            method='POST', 
            data=vetting_request_data
        )
        logger.info(f"CSO Vetting Service responded with status {response.status_code}: {response.text}")
    except Exception as e:
        logger.error(f"Failed to trigger CSO Vetting Service for idea '{idea_id}': {e}")
        # Here you might add logic to update the Firestore status to "VETTING_FAILED"
        return jsonify({"status": "error", "message": "Failed to trigger downstream service."}), 500

    return jsonify({
        "status": "success",
        "message": f"Discovery cycle started. App idea '{idea_id}' is now pending security vetting."
    })

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/discovery_cycle/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/cso_vetting/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/cso_vetting/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cso_vetting/main.py ---

import os
from flask import Flask, request, jsonify
from shared.utils import get_logger
from shared.gcp_client import get_from_firestore, save_to_firestore

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/vet', methods=['POST'])
def vet_idea():
    """
    Receives an app idea ID, retrieves it from Firestore, and performs a mock vetting.
    """
    logger.info("CSO Vetting Service received a request.")
    data = request.get_json()

    if not data or "idea_id" not in data:
        logger.warning("Request missing 'idea_id'.")
        return jsonify({"status": "error", "message": "Missing 'idea_id' in request body."}), 400

    idea_id = data["idea_id"]
    logger.info(f"Starting security vetting for app idea: {idea_id}")

    try:
        # 1. Get the idea from Firestore
        app_idea = get_from_firestore("app_ideas", idea_id)
        if not app_idea:
            return jsonify({"status": "error", "message": f"App idea '{idea_id}' not found."}), 404

        # 2. Perform mock vetting logic
        logger.info(f"Performing security checks on: {app_idea.get('description')}")
        # ... In a real scenario, complex security checks would happen here ...
        
        # 3. Update the status in Firestore
        updated_data = {"status": "VETTING_PASSED"}
        save_to_firestore("app_ideas", idea_id, updated_data)

        logger.info(f"Security vetting for '{idea_id}' passed.")
        return jsonify({"status": "success", "message": f"Vetting passed for '{idea_id}'."})

    except Exception as e:
        logger.error(f"An error occurred during vetting for '{idea_id}': {e}")
        # Optionally, update Firestore status to "VETTING_FAILED"
        return jsonify({"status": "error", "message": "An internal error occurred."}), 500

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cso_vetting/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/ai_developer/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/ai_developer/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/ai_developer/main.py ---

import os
from flask import Flask, jsonify
from shared.utils import get_logger
from shared.gcp_client import get_secret

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/')
def handle_request():
    logger.info("AI Developer Agent Service received a request.")
    # Example of using a secret
    api_key = get_secret("gemini-api-key")
    if not api_key:
        logger.error("Could not retrieve Gemini API key.")
        return jsonify({"status": "error", "message": "Missing secret"}), 500

    # Placeholder for development logic
    logger.info("Development logic would run here.")
    return jsonify({"status": "success", "message": "AI Developer task placeholder complete."})

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/ai_developer/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/cpo_analysis/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/cpo_analysis/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cpo_analysis/main.py ---

import os
from flask import Flask, jsonify
from shared.utils import get_logger

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/')
def handle_request():
    logger.info("CPO Analysis Service received a request.")
    # Placeholder for analysis logic
    logger.info("Product analysis logic would run here.")
    return jsonify({"status": "success", "message": "CPO Analysis task placeholder complete."})

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cpo_analysis/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/cmo_publishing/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/cmo_publishing/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cmo_publishing/main.py ---

import os
from flask import Flask, jsonify
from shared.utils import get_logger
from shared.gcp_client import get_secret

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/')
def handle_request():
    logger.info("CMO Publishing Agent received a request.")
    # Example of using a secret
    api_key = get_secret("google-play-api-key")
    if not api_key:
        logger.error("Could not retrieve Google Play API key.")
        return jsonify({"status": "error", "message": "Missing secret"}), 500

    # Placeholder for publishing logic
    logger.info("Publishing logic would run here.")
    return jsonify({"status": "success", "message": "CMO Publishing task placeholder complete."})

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cmo_publishing/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================



================================================================================

--- File: backend/.env ---



================================================================================

--- File: backend/.env.example ---

# This is an example file. For local development, copy this to .env and fill in the values.
# These are automatically used by Docker Compose for local runs. They are NOT used in GCP.

# The GCP Project ID where the services are running.
GCP_PROJECT="app-factory-v2"

================================================================================

--- File: backend/.gitignore ---

.env


================================================================================

--- File: backend/compose.yaml ---

services:
  mcp-gateway:
    image: docker/mcp-gateway:latest
    ports:
      - "8811:8811"
    command:
      - "--transport=sse"
      - "--servers=github-tool,web-scraper-tool,play-publisher-tool"
  discovery-cycle-service:
    build:
      context: ./services/discovery_cycle
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/discovery-cycle-service
    x-google-run-service:
      service-account: discovery-cycle-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
  cso-vetting-service:
    build:
      context: ./services/cso_vetting
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/cso-vetting-service
    x-google-run-service:
      service-account: cso-vetting-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
  cpo-analysis-service:
    build:
      context: ./services/cpo_analysis
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/cpo-analysis-service
    x-google-run-service:
      service-account: cpo-analysis-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
  ai-developer-agent-service:
    build:
      context: ./services/ai_developer
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/ai-developer-agent-service
    x-google-run-service:
      service-account: ai-developer-agent-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
    x-google-secrets:
      - secret: gemini-api-key
        env: GEMINI_API_KEY
  cmo-publishing-agent:
    build:
      context: ./services/cmo_publishing
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/cmo-publishing-agent
    x-google-run-service:
      service-account: cmo-publishing-agent-sa@app-factory-v2.iam.gserviceaccount.com
      ingress: internal-and-cloud-load-balancing
    x-google-secrets:
      - secret: google-play-api-key
        env: GOOGLE_PLAY_API_KEY
  github-tool:
    image: ghcr.io/github/github-mcp-server:latest
    x-google-secrets:
      - secret: github-token
        env: GITHUB_TOKEN
  web-scraper-tool:
    build:
      context: ./tools/robust_web_scraper
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/web-scraper-tool
    x-google-run-job:
      tasks: 1
      timeout: 3600s
  play-publisher-tool:
    build:
      context: ./tools/google_play_publisher
    image: us-central1-docker.pkg.dev/app-factory-v2/app-factory-repo/play-publisher-tool
    x-google-run-job:
      tasks: 1
      timeout: 3600s

================================================================================

--- File: backend/tools/google_play_publisher/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/tools/google_play_publisher/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/google_play_publisher/main.py ---

from shared.utils import get_logger
from shared.gcp_client import get_secret

logger = get_logger(__name__)

def main():
    logger.info("Google Play Publisher Tool job started.")
    # This would be triggered as a Cloud Run Job
    
    # Placeholder logic
    play_api_key = get_secret("google-play-api-key")
    if not play_api_key:
        logger.error("Failed to get Google Play API key. Exiting.")
        return

    logger.info("Publishing to Google Play Store...")
    # ... actual publishing logic here ...
    logger.info("Google Play Publisher Tool job finished.")

if __name__ == "__main__":
    main()

================================================================================

--- File: backend/tools/google_play_publisher/requirements.txt ---

google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/tools/robust_web_scraper/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/tools/robust_web_scraper/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["python", "main.py"]

================================================================================

--- File: backend/tools/robust_web_scraper/main.py ---

from shared.utils import get_logger

logger = get_logger(__name__)

def main():
    logger.info("Robust Web Scraper Tool job started.")
    # This would be triggered as a Cloud Run Job
    
    # Placeholder logic for scraping
    logger.info("Scraping websites for app ideas...")
    # ... actual scraping logic here ...
    logger.info("Robust Web Scraper Tool job finished.")

if __name__ == "__main__":
    main()

================================================================================

--- File: backend/tools/robust_web_scraper/requirements.txt ---

google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/shared/__init__.py ---

# This file makes the 'shared' directory a Python package.

================================================================================

--- File: backend/shared/gcp_client.py ---

import os
import google.auth.transport.requests
import google.oauth2.id_token
from google.cloud import firestore
from google.cloud import secretmanager
import requests

# Initialize clients globally to reuse connections
project_id = os.environ.get("GCP_PROJECT", "app-factory-v2")
db = firestore.Client(project=project_id)
secret_client = secretmanager.SecretManagerServiceClient()

def get_secret(secret_id: str, version_id: str = "latest") -> str:
    """Retrieves a secret value from Google Secret Manager."""
    name = f"projects/{project_id}/secrets/{secret_id}/versions/{version_id}"
    try:
        response = secret_client.access_secret_version(request={"name": name})
        return response.payload.data.decode("UTF-8")
    except Exception as e:
        print(f"Error accessing secret {secret_id}: {e}")
        return ""

def save_to_firestore(collection: str, doc_id: str, data: dict):
    """Saves or updates a document in a Firestore collection."""
    try:
        doc_ref = db.collection(collection).document(doc_id)
        doc_ref.set(data, merge=True)
        print(f"Successfully saved doc '{doc_id}' to collection '{collection}'.")
    except Exception as e:
        print(f"Error saving to Firestore: {e}")
        raise

def get_from_firestore(collection: str, doc_id: str) -> dict | None:
    """Retrieves a document from a Firestore collection."""
    try:
        doc_ref = db.collection(collection).document(doc_id)
        doc = doc_ref.get()
        if doc.exists:
            return doc.to_dict()
        else:
            print(f"Document '{doc_id}' not found in collection '{collection}'.")
            return None
    except Exception as e:
        print(f"Error retrieving from Firestore: {e}")
        raise

def make_internal_request(service_url: str, method: str = 'GET', data: dict = None) -> requests.Response:
    """
    Makes an authenticated HTTP request to another internal Cloud Run service.
    """
    try:
        auth_req = google.auth.transport.requests.Request()
        id_token = google.oauth2.id_token.fetch_id_token(auth_req, service_url)

        headers = {
            'Authorization': f'Bearer {id_token}',
            'Content-Type': 'application/json'
        }

        if method.upper() == 'POST':
            response = requests.post(service_url, headers=headers, json=data, timeout=30)
        else:
            response = requests.get(service_url, headers=headers, timeout=30)
        
        response.raise_for_status() # Raise an exception for bad status codes
        return response
        
    except Exception as e:
        print(f"Error making internal request to {service_url}: {e}")
        raise

================================================================================

--- File: backend/shared/utils.py ---

import logging
import sys

def get_logger(name: str):
    """
    Configures and returns a standardized logger.
    """
    logger = logging.getLogger(name)
    if not logger.handlers:
        logger.setLevel(logging.INFO)
        handler = logging.StreamHandler(sys.stdout)
        formatter = logging.Formatter(
            '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
        )
        handler.setFormatter(formatter)
        logger.addHandler(handler)
    return logger

================================================================================

--- File: backend/services/discovery_cycle/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/discovery_cycle/Dockerfile ---

# Use an official lightweight Python image.
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /usr/src/app

# Copy the requirements file into the container
COPY requirements.txt ./

# Install the dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the main application file into the container
COPY main.py .

# Command to run the application using Gunicorn
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/discovery_cycle/main.py ---

import os
import uuid
from flask import Flask, request, jsonify
from shared.utils import get_logger
from shared.gcp_client import save_to_firestore, make_internal_request

app = Flask(__name__)
logger = get_logger(__name__)

# The URL for the CSO Vetting Service will be automatically provided by Cloud Run
CSO_VETTING_SERVICE_URL = os.environ.get("CSO_VETTING_SERVICE_URL")

@app.route('/start', methods=['POST'])
def start_cycle():
    """
    Starts a new app discovery cycle.
    1. Creates a new "app_idea" document in Firestore.
    2. Triggers the CSO Vetting Service to begin its process.
    """
    logger.info("Discovery Cycle Service received a request to start.")

    # 1. Generate a new App Idea and save it to Firestore
    idea_id = f"idea-{uuid.uuid4()}"
    app_idea_data = {
        "idea_id": idea_id,
        "status": "PENDING_VETTING",
        "description": "A revolutionary new mobile app for discovering local coffee shops."
        # In a real scenario, this data would come from the web-scraper-tool
    }

    try:
        save_to_firestore("app_ideas", idea_id, app_idea_data)
    except Exception as e:
        logger.error(f"Failed to save new app idea '{idea_id}' to Firestore: {e}")
        return jsonify({"status": "error", "message": "Could not save app idea."}), 500

    # 2. Trigger the CSO Vetting Service
    if not CSO_VETTING_SERVICE_URL:
        logger.error("CSO_VETTING_SERVICE_URL environment variable is not set.")
        return jsonify({"status": "error", "message": "Downstream service is not configured."}), 500

    logger.info(f"Triggering CSO Vetting Service for idea '{idea_id}' at {CSO_VETTING_SERVICE_URL}")

    try:
        vetting_request_data = {"idea_id": idea_id}
        # This is a secure, authenticated call from one Cloud Run service to another
        response = make_internal_request(
            service_url=f"{CSO_VETTING_SERVICE_URL}/vet", 
            method='POST', 
            data=vetting_request_data
        )
        logger.info(f"CSO Vetting Service responded with status {response.status_code}: {response.text}")
    except Exception as e:
        logger.error(f"Failed to trigger CSO Vetting Service for idea '{idea_id}': {e}")
        # Here you might add logic to update the Firestore status to "VETTING_FAILED"
        return jsonify({"status": "error", "message": "Failed to trigger downstream service."}), 500

    return jsonify({
        "status": "success",
        "message": f"Discovery cycle started. App idea '{idea_id}' is now pending security vetting."
    })

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/discovery_cycle/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/cso_vetting/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/cso_vetting/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cso_vetting/main.py ---

import os
from flask import Flask, request, jsonify
from shared.utils import get_logger
from shared.gcp_client import get_from_firestore, save_to_firestore

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/vet', methods=['POST'])
def vet_idea():
    """
    Receives an app idea ID, retrieves it from Firestore, and performs a mock vetting.
    """
    logger.info("CSO Vetting Service received a request.")
    data = request.get_json()

    if not data or "idea_id" not in data:
        logger.warning("Request missing 'idea_id'.")
        return jsonify({"status": "error", "message": "Missing 'idea_id' in request body."}), 400

    idea_id = data["idea_id"]
    logger.info(f"Starting security vetting for app idea: {idea_id}")

    try:
        # 1. Get the idea from Firestore
        app_idea = get_from_firestore("app_ideas", idea_id)
        if not app_idea:
            return jsonify({"status": "error", "message": f"App idea '{idea_id}' not found."}), 404

        # 2. Perform mock vetting logic
        logger.info(f"Performing security checks on: {app_idea.get('description')}")
        # ... In a real scenario, complex security checks would happen here ...
        
        # 3. Update the status in Firestore
        updated_data = {"status": "VETTING_PASSED"}
        save_to_firestore("app_ideas", idea_id, updated_data)

        logger.info(f"Security vetting for '{idea_id}' passed.")
        return jsonify({"status": "success", "message": f"Vetting passed for '{idea_id}'."})

    except Exception as e:
        logger.error(f"An error occurred during vetting for '{idea_id}': {e}")
        # Optionally, update Firestore status to "VETTING_FAILED"
        return jsonify({"status": "error", "message": "An internal error occurred."}), 500

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cso_vetting/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/ai_developer/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/ai_developer/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/ai_developer/main.py ---

import os
from flask import Flask, jsonify
from shared.utils import get_logger
from shared.gcp_client import get_secret

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/')
def handle_request():
    logger.info("AI Developer Agent Service received a request.")
    # Example of using a secret
    api_key = get_secret("gemini-api-key")
    if not api_key:
        logger.error("Could not retrieve Gemini API key.")
        return jsonify({"status": "error", "message": "Missing secret"}), 500

    # Placeholder for development logic
    logger.info("Development logic would run here.")
    return jsonify({"status": "success", "message": "AI Developer task placeholder complete."})

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/ai_developer/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/cpo_analysis/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/cpo_analysis/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cpo_analysis/main.py ---

import os
from flask import Flask, jsonify
from shared.utils import get_logger

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/')
def handle_request():
    logger.info("CPO Analysis Service received a request.")
    # Placeholder for analysis logic
    logger.info("Product analysis logic would run here.")
    return jsonify({"status": "success", "message": "CPO Analysis task placeholder complete."})

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cpo_analysis/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

--- File: backend/services/cmo_publishing/.dockerignore ---

__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env

================================================================================

--- File: backend/services/cmo_publishing/Dockerfile ---

FROM python:3.9-slim
WORKDIR /usr/src/app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY main.py .
CMD ["gunicorn", "--bind", "0.0.0.0:8080", "main:app"]

================================================================================

--- File: backend/services/cmo_publishing/main.py ---

import os
from flask import Flask, jsonify
from shared.utils import get_logger
from shared.gcp_client import get_secret

app = Flask(__name__)
logger = get_logger(__name__)

@app.route('/')
def handle_request():
    logger.info("CMO Publishing Agent received a request.")
    # Example of using a secret
    api_key = get_secret("google-play-api-key")
    if not api_key:
        logger.error("Could not retrieve Google Play API key.")
        return jsonify({"status": "error", "message": "Missing secret"}), 500

    # Placeholder for publishing logic
    logger.info("Publishing logic would run here.")
    return jsonify({"status": "success", "message": "CMO Publishing task placeholder complete."})

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))

================================================================================

--- File: backend/services/cmo_publishing/requirements.txt ---

Flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
google-cloud-secret-manager==2.19.0

================================================================================

